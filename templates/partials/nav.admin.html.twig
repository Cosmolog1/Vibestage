<div class="container">
    <nav>
        <div class="logo">
          <a href = "{{path('home')}}"><img src="/images/Vibestage_logo.png" alt="Logo Vibestage"></a>
        </div>
        <div classe="menu">
            {% if app.user %}
                {% if is_granted("ROLE_ADMIN") %}
                    <a href="{{path('admin')}}">Admin</a>
                {% endif %}
            {% endif %}
        </div>
        <div classe="connect">
            {% if app.user %}
                {{app.user.pseudo}}  <a href="{{path('app_logout')}}">/ Disconnect</a>
                {% else %}
                    <a class="connect" href="{{ path ('app_login') }}">Connect / Register</a>
            {% endif %}
        </div>
    </nav>
</div>


